package com.example.cmoslabopen.measurement.ui

import androidx.compose.runtime.Composable

/**
 * Landing screen. From here the operator either starts a new session
 * (-> [SessionConfigScreen]) or reviews the device's camera inventory.
 */
@Composable
fun MainScreen(
    onStartNewSession: () -> Unit,
) {
    TODO("Phase 11: render main screen")
}
